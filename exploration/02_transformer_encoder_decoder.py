# %% [markdown]
# # 02: Transformer — Encoder, Decoder, and Encoder-Decoder
# Roadmap: Days 3-4
# Prerequisite: 01_attention_from_scratch.py

# %%
# TODO: Build after completing 01
# This file will implement:
# 1. Full transformer block (attention + FFN + LayerNorm + residual)
# 2. Encoder-only model (BERT-style)
# 3. Decoder-only model (GPT-style) — with causal mask
# 4. Cross-attention (the VLM bridge)
# 5. Train tiny GPT on Shakespeare
